//=============================================================================
//
// fs_shadowvol.shd
//
//=============================================================================

void main()
{
	// This is just for debugging output.
    gl_FragColor = gl_FrontFacing ? vec4(1.0, 1.0, 0.0, 1.0) : vec4(0.0, 0.0, 1.0, 1.0);
}

